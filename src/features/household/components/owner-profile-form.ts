export function renderOwnerProfileForm(root: HTMLElement): HTMLFormElement {
  root.innerHTML = `<div class="security-shell"><header class="public-header"><span class="wordmark"><span class="wordmark-mark" aria-hidden="true">UF</span><span>UrbanFox ILR</span></span></header><main class="security-main"><section class="security-card" aria-labelledby="owner-title"><p class="eyebrow">Primary local profile</p><h1 id="owner-title">Tell Freddy about you.</h1><p>Enter only the essential details needed to keep your household records separate. This does not assign an immigration role.</p><form id="owner-form" class="pin-form" novalidate><label for="full-name">Full name</label><input id="full-name" name="fullName" autocomplete="name" maxlength="100" required /><label for="date-of-birth">Date of birth</label><input id="date-of-birth" name="dateOfBirth" type="date" autocomplete="bday" required /><p id="owner-error" class="form-error" role="alert" hidden></p><button class="primary-button" type="submit">Save local profile</button></form><p class="storage-note">These details are encrypted and stored only on this device.</p></section></main></div>`;
  const form = root.querySelector<HTMLFormElement>("#owner-form");
  if (!form) throw new Error("Owner form could not be rendered.");
  return form;
}
