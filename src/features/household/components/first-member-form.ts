export function renderFirstMemberForm(root: HTMLElement): HTMLFormElement {
  root.innerHTML = `<div class="security-shell"><header class="public-header"><span class="wordmark"><img class="wordmark-logo" src="./brand-logo.png" alt="" /><span>UrbanFox ILR</span></span></header><main class="security-main"><section class="security-card" aria-labelledby="member-title"><p class="eyebrow">First household member</p><h1 id="member-title">Who are we tracking first?</h1><p>Every household member is treated equally. Immigration roles describe each person’s route and do not create an app owner.</p><form id="first-member-form" class="pin-form" novalidate><label for="full-name">Full name</label><input id="full-name" name="fullName" autocomplete="name" maxlength="100" required /><label for="date-of-birth">Date of birth</label><input id="date-of-birth" name="dateOfBirth" type="date" autocomplete="bday" required /><label for="immigration-role">Immigration role</label><select id="immigration-role" name="immigrationRole" required><option value="not-set">Not set yet</option><option value="main-applicant">Main applicant</option><option value="dependant">Dependant</option></select><p id="member-error" class="form-error" role="alert" hidden></p><button class="primary-button" type="submit">Create household member</button></form><p class="storage-note">These details are encrypted and stored only on this device.</p></section></main></div>`;
  const form = root.querySelector<HTMLFormElement>("#first-member-form");
  if (!form) throw new Error("First member form could not be rendered.");
  return form;
}
