export const APP_VERSION = "3.22.9";

export const RELEASE_NOTES = [
  "Document checklist items now change from Add to Edit after upload and open the saved document for editing.",
  "Existing document details can be updated without uploading the file again, or the stored file can be replaced when needed.",
] as const;
