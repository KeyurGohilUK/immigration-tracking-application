export const APP_VERSION = "3.25.11";

export const RELEASE_NOTES = [
  "Permission history is now managed directly from the ILR journey page, removing the separate Immigration History screen.",
  "The ILR action is now labelled Add Permission and opens the permission form immediately.",
  "Existing permission cards can now be opened directly for editing, with delete available from the edit dialog.",
] as const;
