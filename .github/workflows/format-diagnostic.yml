name: Format diagnostic

on:
  pull_request:

permissions:
  contents: read

jobs:
  prettier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Show Prettier diff
        run: |
          npx prettier --write src/app/start-application.ts src/features/documents/domain/address-history.ts tests/e2e/app-shell.spec.ts
          git diff -- src/app/start-application.ts src/features/documents/domain/address-history.ts tests/e2e/app-shell.spec.ts
